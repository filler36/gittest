pipeline {
    agent any
    stages {
        stage('Greetings222') {
            steps {
                sh 'echo "Hello World"'
            }
        }
        stage('OS release') {
            steps {
                sh 'cat /etc/os-release'
            }
        }
    }
}
